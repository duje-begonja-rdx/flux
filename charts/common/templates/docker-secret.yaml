{{- if .Values.docker_secret.enabled -}}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  # name of ExternalSecret under CRD section
  name: {{ .Values.docker_secret.secret_name }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: main
    kind: ClusterSecretStore
  target:
    # name of actual secret under Config/Secrets, not required will use .metadata.name
    creationPolicy: Owner
    template:
      type: kubernetes.io/dockerconfigjson
  data:
  - secretKey: .dockerconfigjson
    remoteRef:
      key: {{ .Values.docker_secret.name }}
      version: "AWSCURRENT"
      property: dockerconfigjson
{{- end }}